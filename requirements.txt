flask
pymupdf
werkzeug
python-dotenv
psycopg2-binary
rapidfuzz
fpdf2
pytesseract
Pillow

# AI-backed chatbot (chatbot.py / global_chatbot.py) - calls Gemini's REST
# API directly via urllib, so no extra SDK dependency is required beyond
# python-dotenv (already above) for loading GEMINI_API_KEY from .env.
